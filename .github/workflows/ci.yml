name: CI Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: accountability unit tests
    runs-on:
      - self-hosted
      - windows
      - x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Start SQL Server engine
        shell: pwsh
        run: docker run -d --name sqlci -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=${{ secrets.SA_PASSWORD }}" -p 1433:1433 mcr.microsoft.com/mssql/server:2022-latest

      - name: Wait for SQL Server to be ready
        shell: pwsh
        run: |
          $pw = '${{ secrets.SA_PASSWORD }}'
          do {
            Start-Sleep -Seconds 1
            docker exec sqlci /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P $pw -Q "SELECT 1" > $null 2>&1
          } until ($LASTEXITCODE -eq 0)

      - name: Build Docker image
        run: docker build -t accountability-ci-pipeline .

      - name: Run tests
        run: |
          docker run --rm \
            -e DB_SERVER=host.docker.internal \
            -e DB_USERNAME=sa \
            -e DB_PASSWORD="${{ secrets.SA_PASSWORD }}" \
            -e DB_NAME=master \
            accountability-ci-pipeline

      - name: Tear down SQL Server
        if: always()
        run: docker rm -f sqlci

# name: CI Pipeline

# on:
#   pull_request:
#     branches:
#       - main

# jobs:
#   build:
#     name: accountability unit tests
#     #runs-on: ubuntu-latest
#     runs-on:
#     - self-hosted
#     - windows
#     - x64
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Build Docker image
#         run: |
#           docker build -t myapp -f ./Dockerfile .

#       - name: Run tests with pytest inside Docker container
#         run: |
#           docker run --rm myapp pytest


  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.event.pull_request.merged == true

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2

  #     - name: Set up Azure CLI
  #       uses: azure/setup-azurecli@v1
  #       with:
  #         version: '2.28.0'

  #     - name: Install Azure DevOps CLI extension
  #       run: |
  #         az extension add --name azure-devops

  #     - name: Deploy to Azure DevOps
  #       uses: azure/cli-action@v2
  #       with:
  #         azcliversion: '2.28.0'
  #       env:
  #         AZURE_DEVOPS_EXT_PATVAR: ${{ secrets.AZURE_DEVOPS_PAT }}
  #       run: |
  #         az devops login --token $AZURE_DEVOPS_EXT_PATVAR
  #         git remote add azure https://dev.azure.com/your_organization/your_project/_git/your_repo
  #         git push azure main