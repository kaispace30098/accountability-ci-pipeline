name: CI Pipeline

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Accountability Unit Tests
    runs-on:
      - self-hosted
      - windows
      - x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Start SQL Server engine (CI-only)
        run: |
          docker run -d \
            --name sqlci \
            -e "ACCEPT_EULA=Y" \
            -e "MSSQL_SA_PASSWORD=${{ secrets.SA_PASSWORD }}" \
            -p 1433:1433 \
            mcr.microsoft.com/mssql/server:2022-latest
        # Pulls Microsoft’s SQL Server image and runs it as 'sqlci' :contentReference[oaicite:0]{index=0}

      - name: Wait for SQL Server to initialize
        run: Start-Sleep -Seconds 30
        # Gives SQL Server time to come online :contentReference[oaicite:1]{index=1}

      - name: Build application Docker image
        run: docker build -t accountability-ci-pipeline .
        # Creates 'accountability-ci-pipeline' image from our Dockerfile :contentReference[oaicite:2]{index=2}

      - name: Run pytest integration tests
        run: |
          docker run --rm \
            -e DB_SERVER=sqlci \
            -e DB_NAME=master \
            -e DB_USERNAME=sa \
            -e DB_PASSWORD="${{ secrets.SA_PASSWORD }}" \
            accountability-ci-pipeline
        # Overrides defaults so tests hit the fresh SQL Server container named 'sqlci' :contentReference[oaicite:3]{index=3}

      - name: Tear down SQL Server
        if: always()
        run: docker rm -f sqlci

# name: CI Pipeline

# on:
#   pull_request:
#     branches:
#       - main

# jobs:
#   build:
#     name: accountability unit tests
#     #runs-on: ubuntu-latest
#     runs-on:
#     - self-hosted
#     - windows
#     - x64
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Build Docker image
#         run: |
#           docker build -t myapp -f ./Dockerfile .

#       - name: Run tests with pytest inside Docker container
#         run: |
#           docker run --rm myapp pytest


  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.event.pull_request.merged == true

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2

  #     - name: Set up Azure CLI
  #       uses: azure/setup-azurecli@v1
  #       with:
  #         version: '2.28.0'

  #     - name: Install Azure DevOps CLI extension
  #       run: |
  #         az extension add --name azure-devops

  #     - name: Deploy to Azure DevOps
  #       uses: azure/cli-action@v2
  #       with:
  #         azcliversion: '2.28.0'
  #       env:
  #         AZURE_DEVOPS_EXT_PATVAR: ${{ secrets.AZURE_DEVOPS_PAT }}
  #       run: |
  #         az devops login --token $AZURE_DEVOPS_EXT_PATVAR
  #         git remote add azure https://dev.azure.com/your_organization/your_project/_git/your_repo
  #         git push azure main